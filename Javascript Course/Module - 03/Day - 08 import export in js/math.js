export const add = (a, b) => a + b;

export const subtract = (a, b) => a - b; //named exports


const multiply=(a,b)=>a*b;
export default multiply;  //default export (it can be renamed in the new file)